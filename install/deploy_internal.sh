#!/bin/bash
set -e

echo "[Deploy] Internal pilot deploy starting..."

# Placeholder: you can later add rsync/SSH or container deploy here.
echo "[Deploy] This is an internal-only deploy hook. No external changes performed."

echo "[Deploy] Internal pilot deploy complete."
