# freedomlink-ts-lib

A simple TypeScript library scaffolded in the Freedomlink workspace.

## Scripts

- `npm run build` - Compile TypeScript into `dist`
- `npm run clean` - Remove the built output

## Usage

```ts
import { hello } from "freedomlink-ts-lib";

console.log(hello("Freedomlink"));
```
